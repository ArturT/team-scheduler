<h1 class="page-header">Edit Schedule</h1>

<%= render 'form', :params => {:url => project_schedule_path(@project.id, @schedule.id)}, :schedule => @schedule, :project => @project, :developer => @developer, :default_hours => @default_hours %>